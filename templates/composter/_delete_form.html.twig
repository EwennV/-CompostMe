<form method="post" action="{{ path('app_composter_delete', {'id': composter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ composter.id) }}">
    <button class="btn">Delete</button>
</form>
