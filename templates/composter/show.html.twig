{% extends 'base.html.twig' %}

{% block title %}Composter{% endblock %}

{% block body %}
    <h1>Composter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ composter.id }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ composter.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ composter.longitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_composter_index') }}">back to list</a>

    <a href="{{ path('app_composter_edit', {'id': composter.id}) }}">edit</a>

    {{ include('composter/_delete_form.html.twig') }}
{% endblock %}
