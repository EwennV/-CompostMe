{% extends 'base.html.twig' %}

{% block title %}AccessType{% endblock %}

{% block body %}
    <h1>AccessType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ access_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ access_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_access_type_index') }}">back to list</a>

    <a href="{{ path('app_access_type_edit', {'id': access_type.id}) }}">edit</a>

    {{ include('access_type/_delete_form.html.twig') }}
{% endblock %}
