<form method="post" action="{{ path('app_access_type_delete', {'id': access_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ access_type.id) }}">
    <button class="btn">Delete</button>
</form>
